/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,jsx,ts,tsx}" // scan all your React source files
    ],
    theme: {
        extend: {}, // add custom colors, spacing, fonts, etc.
    },
    plugins: [],
};
